# Chatbot-Onboarding-V2

A lightweight app-shell wrapper for the chatbot onboarding prototype.

## Run locally

From the workspace root, serve the folder with Python:

```bash
python -m http.server 8010
```

Then open:

- http://localhost:8010/
- http://localhost:8010/app.html
